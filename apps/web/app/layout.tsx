import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "TSA | Technical Stewardship Academy",
    template: "%s | TSA",
  },
  description: "Technical Stewardship Academy — learn, build, improve, and lead through practical engineering progression.",
  applicationName: "Technical Stewardship Academy",
  icons: {
    icon: "/tsa-mark.svg",
    shortcut: "/tsa-mark.svg",
    apple: "/tsa-mark.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
