import type { Lesson } from "./lesson";

function practical(id: string, title: string, objective: string, scenario: string, instructions: string[], deliverables: string[], completionCriteria: string[], estimatedMinutes = 120): Lesson {
    return { id, title, activities: [{ id: `${id}-lab`, title, estimatedMinutes, content: { type: "practical", objective, scenario, instructions, deliverables, completionCriteria } }] };
}

export const soapXmlEnterpriseIntegrationDeepLessons: Lesson[] = [
    practical("soap-xml-document-model", "XML Documents, Namespaces and Parsing", "Read, create and troubleshoot XML as a structured namespaced document format rather than treating it as verbose JSON.", "A legacy enterprise CMDB will become a Steward integration dependency. Its messages are XML and use multiple namespaces, so string manipulation is not a safe integration strategy.", ["Create representative service, owner and environment XML documents with declarations, attributes, nested elements and escaped content.", "Introduce a namespace for the CMDB contract and a second namespace for shared enterprise identifiers; inspect qualified names and namespace prefixes.", "Parse the documents with a real XML parser and query required values without depending on a particular prefix spelling.", "Compare elements versus attributes and identify where ordering, namespaces and mixed/document semantics make XML different from JSON.", "Create malformed XML and a well-formed document with the wrong namespace; capture and explain the different failures.", "Record safe parser expectations for untrusted XML, including why external-entity behavior must not be enabled casually."], ["Representative XML documents", "Namespace map", "Parser/query implementation", "Malformed-versus-wrong-namespace evidence", "XML safety note"], ["The learner can explain well-formedness independently of schema validity.", "Namespace identity is not confused with prefix text.", "XML is parsed structurally rather than with regex/string slicing.", "Unsafe external-entity assumptions are identified."]),
    practical("soap-xml-xsd-contract", "XSD: Make the XML Contract Executable", "Define and validate an XML Schema that captures the structural contract for the enterprise service messages.", "The CMDB team says a sample XML file is the contract. Steward needs machine-checkable rules for required fields, types, cardinality and namespace membership before integrating.", ["Create an XSD for the selected service-record message and declare its target namespace deliberately.", "Model required/optional elements, suitable simple types and at least one repeating structure or bounded cardinality.", "Validate a correct XML instance against the XSD with a real validator.", "Create invalid instances for a missing required value, wrong type/cardinality and wrong namespace and capture validation diagnostics.", "Change the schema in one backward-compatible and one breaking way and predict the effect on an existing consumer.", "Document what XSD proves and what business semantics still require application/domain validation."], ["Versioned XSD", "Valid instance", "Three invalid-instance results", "Compatibility change note", "Schema-versus-domain-rule boundary"], ["Validation is executed, not merely described.", "Namespace and cardinality failures are demonstrated.", "Schema validation is not presented as complete business validation.", "Compatibility reasoning uses actual consumer impact."]),
    practical("soap-wsdl-contract", "WSDL and the SOAP Service Contract", "Trace how WSDL connects operations, XML messages, bindings and network endpoints before generating or implementing code.", "The CMDB exposes a SOAP service. The integration cannot be understood safely from a URL and one captured envelope alone.", ["Create or inspect a WSDL 1.1 contract for a bounded operation such as GetServiceRecord and one mutation such as UpdateServiceOwner.", "Trace types/schema → messages → portType/operations → binding → service/port endpoint and draw the relationship.", "Identify SOAP action/binding expectations and the request/response XSD types used by each operation.", "Use a WSDL-aware client/tool or library to inspect the published contract and enumerate operations from the contract rather than handwritten notes.", "Change an operation or message contract deliberately and observe how the client/tool exposes the incompatibility.", "Record transport concerns separately from operation/business semantics."], ["Versioned WSDL/XSD contract", "WSDL component map", "Operation inventory from tooling", "Breaking-contract evidence", "Transport-versus-semantic boundary note"], ["The learner can navigate a WSDL without treating it as opaque generated XML.", "Operations resolve to explicit schema types and bindings.", "A contract incompatibility is reproduced.", "WSDL is understood as more than endpoint documentation."]),
    practical("soap-envelope-faults", "SOAP Envelopes, Headers and Faults", "Construct and diagnose SOAP messages at the wire level before hiding them behind generated client abstractions.", "A generated client reports a generic integration error. Operators need to determine whether the wire message, namespace, SOAP header, business request or remote service failed.", ["Send a valid SOAP request and capture the raw HTTP request/response including Content-Type/SOAPAction behavior appropriate to the chosen SOAP version/binding.", "Identify Envelope, Header and Body and map the operation payload to its XSD namespace.", "Add one bounded correlation/request identifier in a SOAP header and verify the service can observe it.", "Trigger a deliberate application rejection represented as a SOAP Fault and distinguish fault code/category, fault reason and application detail.", "Send a wrong operation namespace or malformed envelope and compare protocol/contract failure with the application fault.", "Create a diagnostic checklist from HTTP/network → SOAP envelope/binding → XML/XSD → operation/business fault."], ["Raw successful SOAP exchange", "Header/correlation evidence", "SOAP Fault evidence", "Protocol-versus-application failure comparison", "SOAP diagnostic checklist"], ["The learner can read the raw envelope despite using libraries later.", "SOAP Fault is not treated as an ordinary successful payload.", "At least two different failure layers are distinguished.", "Correlation survives the SOAP boundary."]),
    practical("soap-service-implementation", "Implement a Contract-First SOAP Endpoint", "Build a working SOAP provider from the reviewed WSDL/XSD contract and keep transport mapping outside Steward domain rules.", "To understand the provider side rather than only consuming legacy services, TSA will implement a small enterprise CMDB SOAP service that owns a bounded external service-record capability.", ["Select a maintained SOAP-capable library/runtime suitable for the learner environment and record the choice; do not hand-build an XML-over-HTTP framework.", "Implement the WSDL/XSD operations against a small deterministic data source or bounded service layer.", "Serve/publish the WSDL and prove a fresh client can discover the endpoint from it.", "Validate/map request data at the SOAP boundary and keep the core operation behavior independent of XML element manipulation.", "Return a typed successful response and a deliberate SOAP Fault for a known rejection/not-found case.", "Restart the service and repeat contract discovery and a representative operation from a separate client process."], ["Runnable SOAP provider", "Published WSDL/XSD", "Successful operation evidence", "Typed SOAP Fault evidence", "Transport/domain boundary map", "Restart/reconnect evidence"], ["The endpoint is actually callable through SOAP.", "The published contract describes the implementation used by the client.", "Domain/service behavior is not embedded in raw XML string handling.", "Success and fault paths both work after restart."]),
    practical("soap-steward-consumer", "Integrate Steward with the SOAP Provider", "Consume the SOAP contract from Steward through an explicit adapter that prevents transport-specific XML concerns from leaking into the domain model.", "Steward needs authoritative metadata from the enterprise CMDB, but its core model and REST API should not become SOAP-shaped merely because the external system is SOAP-based.", ["Generate/configure a SOAP client from the WSDL or use a contract-aware library rather than manually concatenating envelopes for application code.", "Create a Steward integration adapter that maps SOAP contract types to a small internal application/domain representation.", "Call the real SOAP provider from a representative Steward workflow and preserve correlation evidence.", "Map a SOAP Fault, timeout/unavailability and invalid response/contract condition into explicit integration outcomes; do not silently convert all failures into empty data.", "Prove one Steward/domain test can run against the adapter boundary without constructing SOAP XML.", "Document timeout, retry/idempotency and source-of-truth assumptions for the integration."], ["Steward SOAP adapter", "End-to-end provider→Steward evidence", "Fault/error mapping", "Domain-level adapter test", "Integration operating contract"], ["Steward consumes the real SOAP endpoint.", "SOAP/XML types stop at the adapter boundary.", "Failure classes remain distinguishable.", "Retry behavior is justified by operation semantics."]),
    practical("soap-contract-testing-failure", "Contract Testing and Compatibility Failure", "Detect SOAP/XML compatibility problems before they become ambiguous production integration failures.", "The CMDB provider evolves independently. Steward must know which WSDL/XSD changes it can tolerate and which require coordinated release work.", ["Automate validation of representative request/response XML against the versioned schema/contract.", "Create a provider/consumer compatibility check around at least one operation used by Steward.", "Inject a breaking XSD/WSDL change such as namespace/type/required-element incompatibility and prove the check fails before accepting the new contract.", "Inject a compatible additive change and determine whether the actual client tolerates it rather than assuming it does.", "Capture raw messages and contract versions in diagnostics without leaking secrets.", "Define contract ownership, version publication and change-notification expectations."], ["Automated contract validation", "Breaking-change failure evidence", "Additive-change evidence", "Diagnostic artifact example", "Contract ownership/versioning policy"], ["Compatibility is tested against the real client/provider boundary.", "Breaking and additive changes are distinguished empirically.", "Diagnostics identify the contract version involved.", "Contract evolution has an owner and coordination rule."]),
    practical("soap-xml-milestone", "Milestone: Build and Integrate a Contract-First SOAP Service", "Prove the complete enterprise SOAP capability by operating both sides of a real contract and recovering from contract and runtime failures.", "An enterprise CMDB exposes SOAP while Steward remains a REST-oriented service registry. Deliver the integration without making SOAP a permanent architectural default or leaking its model throughout Steward.", ["Start both the SOAP provider and Steward integration from versioned configuration/contracts and record the exact WSDL/XSD versions.", "From a separate client, discover the provider contract and execute a successful operation.", "Execute the representative Steward workflow that consumes the SOAP operation through the adapter and prove the mapped result reaches the intended application behavior.", "Trigger and diagnose one SOAP Fault, one namespace/schema incompatibility and one provider-unavailable condition using the layered diagnostic evidence.", "Restore the healthy contract/runtime and prove the end-to-end workflow recovers.", "Produce a concise runbook covering contract location/version, endpoint discovery, raw-message capture, validation, correlation, common faults and escalation ownership."], ["Running contract-first SOAP provider", "Steward SOAP consumer/adapter", "Successful end-to-end evidence", "Three failure/recovery records", "SOAP/XML integration runbook"], ["Both provider and consumer are implemented and operated.", "XML/XSD/WSDL/SOAP evidence is visible end to end.", "Three distinct failure classes are diagnosed from evidence and recovered.", "SOAP remains isolated at an explicit integration boundary."]),
    practical("soap-rest-reassessment", "Reassess SOAP, REST and the Integration Boundary", "Use the implemented integration to decide where SOAP belongs, where REST belongs and when translation should isolate an enterprise contract.", "The SOAP integration now works. The remaining question is architectural ownership, not whether SOAP is 'old' or REST is 'modern'.", ["Compare the implemented SOAP contract with Steward's REST API across contract strictness, tooling, error semantics, evolvability, observability and organizational ownership.", "Identify the concrete reasons the external boundary remains SOAP and whether any justify exposing SOAP inside Steward itself.", "Review the adapter/anti-corruption boundary and list what would leak if SOAP-generated types were allowed into the domain model.", "Define evidence that would justify provider migration or a façade later, including ownership and consumer coordination costs.", "Record the decision without adding a second permanent integration platform merely for technology exposure."], ["SOAP-versus-REST evidence matrix", "Integration-boundary ADR", "Migration/façade trigger criteria"], ["The decision follows observed requirements and evidence.", "SOAP is neither rejected nor selected because of age/fashion.", "The adapter boundary has an explicit purpose.", "No unnecessary middleware/tool zoo is introduced."], 90),
];
